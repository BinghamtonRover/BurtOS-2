#ifndef NETWORK_H
#define NETWORK_H

//Figure out what to put here

enum class MessageType {
    STATUS,
    ARM,
    DRIVE,
    LOG,
    CAMERA,
    CAMERA_CONTROL,
    COUNT //Count is included so we can interate through this enum
};

//Create some table that binds message types to event handlers



#endif